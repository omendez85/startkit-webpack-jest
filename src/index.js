import './styles/index.scss';

export const suma = (a, b) => {
    if (a === 10) {
        return a;
    }
    return a + b;
    //     document.addEventListener('DOMContentLoaded', () => {
    //     console.log('BUUUUUU');
    // });
};
